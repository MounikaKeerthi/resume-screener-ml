# Resume Matcher V2 - Dependencies
# Install with: pip install -r requirements.txt

# PDF parsing
PyMuPDF==1.24.0

# NLP
spacy==3.7.4
# After pip install, also run: python -m spacy download en_core_web_sm

# V2 UPGRADE: Sentence Transformers (replaces TF-IDF for similarity)
sentence-transformers==3.0.1
# This will also install: torch, transformers, huggingface-hub

# Kept from V1 (still used for skill matching)
scikit-learn==1.5.0

# Web framework + UI
flask==3.0.3
streamlit==1.36.0
requests==2.32.3